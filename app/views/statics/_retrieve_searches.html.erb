<table>
	<thead>
		<tr>
			<th>The Name</th>
		</tr>
	</thead>

	<tbody>
		 <% @search_results_posts.each_with_index do |coche, index| %>
			<tr>
				<td><%= index + 1 %>.</td>
				<td><%= coche.car %></td>
				<td><%= coche.origin %></td>
			</tr>
		 <% end %>
	</tbody>
</table>
